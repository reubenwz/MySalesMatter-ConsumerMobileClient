import { CreateCategoryReq } from './create-category-req';

describe('CreateCategoryReq', () => {
  it('should create an instance', () => {
    expect(new CreateCategoryReq()).toBeTruthy();
  });
});
