import { UpdateCategoryReq } from './update-category-req';

describe('UpdateCategoryReq', () => {
  it('should create an instance', () => {
    expect(new UpdateCategoryReq()).toBeTruthy();
  });
});
